define([
        'underscore',
        'marionette',
        'vent'
    ], function(_, Marionette, vent) {

        'use strict';

        var azTView = Marionette.ItemView.extend({

            });

        return azTView;
    });