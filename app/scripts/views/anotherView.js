define([
        'underscore',
        'marionette',
        'vent'
    ], function(_, Marionette, vent) {

        'use strict';

        var another = Marionette.ItemView.extend({

            });

        return another;
    });