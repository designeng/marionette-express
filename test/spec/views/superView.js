define(["views/superView"], function(superView) {
  	describe("views::superView", function () {
  	
  		var t = new superView();

/* Expectations:

		expect(t.render).
		expect(t.onShow).
		expect(t.onClose).
		expect(t.onChange).

*/


    	describe('your_description', function () {
      		it('your_text', function () {
        


      	});
    });

  });
});
